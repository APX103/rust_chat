# Mini-Agent Configuration
# Place this file at ~/.mini-agent/config.toml

[model]
provider = "openai"
model = "glm-5-turbo"
api_key = "sk-your-key-here"
base_url = "https://api.openai.com/v1"
max_tokens = 4096
temperature = 0.7
top_p = 1.0
timeout = 120          # Request timeout in seconds

# Optional: extra headers for providers like OpenRouter
# [model.extra_headers]
# HTTP-Referer = "https://your-app.com"
# X-Title = "Mini-Agent"

[memory]
enabled = true
provider = "builtin"   # "builtin" | "none"
semantic_search_top_k = 5
episodic_summary_threshold = 10
hybrid_search = true   # FTS5 BM25 + weighted scoring

[agent]
max_iterations = 30
enable_reasoning = true

[observer]
enabled = true
kind = "log"           # "log" | "none"

[heartbeat]
enabled = false
interval_secs = 3600   # Run tasks every hour
tasks = ["auto_summarize", "memory_cleanup"]

# MCP Servers — tools will be auto-discovered and registered
[mcp_servers]

[mcp_servers.time]
command = "uvx"
args = ["mcp-server-time"]
timeout = 30

[mcp_servers.filesystem]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
timeout = 30

# HTTP-based MCP server example
# [mcp_server.remote_api]
# url = "https://mcp.example.com/mcp"
# timeout = 60
