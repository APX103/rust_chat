[package]
name = "mcp-filesystem-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
